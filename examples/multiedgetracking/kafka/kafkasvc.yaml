apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  annotations:
    advertised_ip: 7.182.9.110
  labels:
    name: kafka
spec:
  ports:
  - port: 9092
    name: kafka-port
    protocol: TCP
  selector:
    app: kafka
    id: "0"
  type: LoadBalancer
